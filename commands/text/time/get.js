const { description } = require("./set");

module.exports = {
	name: "get",
	parent: "time",
	description: "gets the time from a mentioned user",
	execute(message, args) {
		message.reply("this is a wip dont wanna work on this right now lol")

	}
}
